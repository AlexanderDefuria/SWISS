
var data = {

}

/*
function toggle_selection(menu) {
    var popup = menu.concat('_menu');
    var td_list = document.querySelectorAll("td")
    for(let i = 0; i < td_list.length; i++){
        console.log(getComputedStyle(td_list[i],null).display);


        // If the element is visible...
        if(getComputedStyle(td_list[i],null).display !== "none"){
            // If the element is not the menu button (ex "Change Event")
            // Then make it invisible
            if(td_list[i].id !== menu)
                td_list[i].style.display = "none";


        } else {
            // If the element is invisible and it is the popup
            if(td_list[i].id === popup){
                td_list[i].style.display = "flex";
            } else {
                // The Blank string will force the element to go back to the CSS default
                td_list[i].style.display = "";
            }
        }
    }
} */

function select_event(event_id){
    var data
}